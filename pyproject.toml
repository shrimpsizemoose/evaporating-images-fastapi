[project]
name = "evaporating-images-fastapi"
version = "0.1.0"
description = "evaporating images with fastapi"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.121.3",
    "redis>=7.1.0",
    "uvicorn>=0.38.0",
    "websockets>=15.0.1",
]
